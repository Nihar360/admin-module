spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: INFO
    com.ecommerce.admin: INFO
    org.springframework.security: WARN

server:
  port: ${PORT:8080}
  error:
    include-message: never
    include-binding-errors: never
    include-stacktrace: NEVER
    # or use lowercase: never

app:
  cors:
    allowed-origins: ${ALLOWED_ORIGINS}